In preparation for part 2 of the shopping cart activity i fixed the version 1.0 with regards to the feedback given by the proffesor. 
I also cleaned the code by removing the first item display that ended up being cleared anyway.
Added the missing methods
Moved the item handling into the class itself.


AI usage in this push:
 After adding the new methods and moving some of the code into the class i had help from AI to fix a problem where stock keeps getting deducted even if the purchase is cancelled due to the cart being full, AI's solution was simply to move the deduct part of the code below the code that checks if the cart is full.
 Also will have help from AI on the git commands that are to be used when pushing to a branch rather than main.
Cleaned the code further, simply moved the codes for displaying items and cart into a method so that it only has to be called with one line in main.


AI usage in this push:
 AI pointed out and explained why the display methods i created has to be inside the program class rather than outside.
 Also will have help from AI on the git commands that are to be used when pushing to a branch rather than main.